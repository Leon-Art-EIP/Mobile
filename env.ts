// BASE URL :
  // ** Contains running server port **

  // If the server is running on PORT 5000 for example,
  // the Base URL is set like this :

  // 'http://10.0.2.2:5000/api/auth/login'

const variables = {
    API_URL: 'http://10.0.2.2:5001/',
  };
  
  export default variables;
  